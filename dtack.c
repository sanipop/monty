#include "monty.h"
/**
 * asanif_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
 */
void asanif_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
